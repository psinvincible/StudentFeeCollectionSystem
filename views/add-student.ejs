<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include("./partials/head") %>
    <title>SFCS - Add-fee</title>
  </head>
  <body>
    <div class="background-theme">
      <div class="shrink-0">
      <%- include("./partials/navbar") %>
      </div>
      <form action="<%= student ? '/student/update-student/' + student._id : '/student/add-student'%>" method="post" class="max-w-md mx-auto mt-10 bg-white p-6 rounded-lg shadow-lg shadow-blue-500" enctype="multipart/form-data">
        <div class="m-4">
          <h2 class="font-bold mb-2"><%= student ? "Edit-Student Form" : "Student-Registration Form" %></h2>
          <div class="mb-3">
            <label for="fullName" class="block text-sm font-medium text-gray-700 mb-1">Student's Full Name</label>
            <input
              type="text"
              class="w-full rounded-md border border-gray-300 px-3 py-2 text-sm focus:border-blue-500 focus:outline-none focus:ring-1 focus:ring-blue-500"
              id="studentName"
              name="studentName"
              value="<%= student ? student.fullName : '' %>"
              placeholder="Enter Full Name"
              required
            />
          </div>
          <div class="mb-3">
            <label for="class" class="block text-sm font-medium text-gray-700 mb-1">Class</label>
            <input
              type="number"
              class="w-full rounded-md border border-gray-300 px-3 py-2 text-sm focus:border-blue-500 focus:outline-none focus:ring-1 focus:ring-blue-500"
              id="class"
              name="class"
              value="<%= student ? student.class : '' %>"
              placeholder="Enter Class"
              required
            />
          </div>
          <div class="mb-3">
            <label for="school" class="block text-sm font-medium text-gray-700 mb-1">School</label>
            <input
              type="text"
              class="w-full rounded-md border border-gray-300 px-3 py-2 text-sm focus:border-blue-500 focus:outline-none focus:ring-1 focus:ring-blue-500"
              id="school"
              name="school"
              value="<%= student ? student.school : '' %>"
              placeholder="Enter School Name"
              required
            />
          </div>
          <div class="mb-3">
            <label for="profileImg" class="block text-sm font-medium text-gray-700 mb-1">Profile Image</label>
            <input
              type="file"
              class="w-full rounded-md border border-gray-300 px-3 py-2 text-sm focus:border-blue-500 focus:outline-none focus:ring-1 focus:ring-blue-500"
              id="profileImg"
              name="profileImg"
              value="<%= student ? student.profileImgURL : '' %>"
            />
            <% if (student && student.profileImgURL) { %>
             <img src="<%= student.profileImgURL %>" alt="profileImg" class="rounded-circle m-2" height="40px" width="40px" >
             <input type="hidden" name="oldProfileImg" value="<%= student.profileImgURL %>" />
            <% } %>
          </div>
          <div class="mb-3">
            <label for="address" class="block text-sm font-medium text-gray-700 mb-1">Address</label>
            <input
              type="text"
              class="w-full rounded-md border border-gray-300 px-3 py-2 text-sm focus:border-blue-500 focus:outline-none focus:ring-1 focus:ring-blue-500"
              id="address"
              name="address"
              value="<%= student ? student.address : '' %>"
              placeholder="Enter address"
              required
            />
          </div>
          <div class="mb-3">
            <label for="contact" class="block text-sm font-medium text-gray-700 mb-1">Contact No.</label>
            <input
              type="text"
              class="w-full rounded-md border border-gray-300 px-3 py-2 text-sm focus:border-blue-500 focus:outline-none focus:ring-1 focus:ring-blue-500"
              id="contact"
              name="contact"
              value="<%= student ? student.contactNo : '' %>"
              placeholder="Mobile Number"
              required
            />
          </div>
          <div class="mb-3">
            <label for="doj" class="block text-sm font-medium text-gray-700 mb-1">Date of Joining</label>
            <input
              type="date"
              class="w-full rounded-md border border-gray-300 px-3 py-2 text-sm focus:border-blue-500 focus:outline-none focus:ring-1 focus:ring-blue-500"
              id="doj"
              name="doj"
              value="<%= student ? student.dateOfJoin.toISOString().split('T')[0] : '' %>"
              placeholder="Select DOJ"
              required
            />
          </div>
          
          <button type="submit" class="w-full rounded-md bg-blue-600 py-2 text-sm font-semibold text-white hover:bg-blue-700 transition"><%= student ? 'Update' : 'Submit' %></button>
        </div>
      </form>
      <div class="shrink-0">
      <%- include("./partials/footer") %>
      </div>
    </div>
  </body>
</html>
